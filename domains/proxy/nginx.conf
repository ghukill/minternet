# Use Docker's internal DNS resolver for dynamic resolution
resolver 127.0.0.11 valid=10s;

server {
    listen 80;
    server_name minternet.test;

    location / {
        set $upstream_minternet http://minternet:80;
        proxy_pass $upstream_minternet;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name recipes.test;

    location / {
        set $upstream_recipes http://recipes:80;
        proxy_pass $upstream_recipes;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name science.test;

    location / {
        set $upstream_science http://science:80;
        proxy_pass $upstream_science;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name blogs.test;

    location / {
        set $upstream_blogs http://blogs:80;
        proxy_pass $upstream_blogs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name wowser.test;

    location / {
        set $upstream_wowser http://wowser:80;
        proxy_pass $upstream_wowser;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Default server - returns 404 for unknown hosts
server {
    listen 80 default_server;
    server_name _;
    return 404;
}
